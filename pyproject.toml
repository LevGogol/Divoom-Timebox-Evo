[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "divoom-timebox-evo"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["run"]
packages = ["app"]

[project.scripts]
timebox = "run:main"
